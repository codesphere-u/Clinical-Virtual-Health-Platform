name: aura_appointments
description: Appointment models, slot booking components and waiting room states for DOCAAS Clinical
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  aura_core:
    path: ../core
  docaas_design_system:
    path: ../design_system

dev_dependencies:
  flutter_lints: ^3.0.0
