name: aura_core
description: Core primitives, errors, and base utilities for DOCAAS Clinical Flutter applications
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  meta: ^1.11.0

dev_dependencies:
  lints: ^3.0.0
