name: docaas_networking
description: Resilient HTTP client with correlation IDs and idempotency headers for DOCAAS Clinical
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  aura_core:
    path: ../core

dev_dependencies:
  flutter_lints: ^3.0.0
